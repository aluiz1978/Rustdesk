name: TCE-PE Custom Build (Windows e Mac)

on:
  workflow_dispatch:

env:
  SCITER_RUST_VERSION: "1.75"
  RUST_VERSION: "1.75"
  MAC_RUST_VERSION: "1.81"
  LLVM_VERSION: "15.0.6"
  FLUTTER_VERSION: "3.24.5"
  TAG_NAME: "nightly"
  VCPKG_BINARY_SOURCES: "clear;x-gha,readwrite"
  VCPKG_COMMIT_ID: "120deac3062162151622ca4860575a33844ba10b"
  VERSION: "1.4.6"
  UPLOAD_ARTIFACT: "true"

jobs:
  generate-bridge:
    uses: ./.github/workflows/bridge.yml

  build-RustDeskTempTopMostWindow:
    uses: ./.github/workflows/third-party-RustDeskTempTopMostWindow.yml
    with:
      upload-artifact: true
      target: windows-2022
      configuration: Release
      platform: x64
      target_version: Windows10
    strategy:
      fail-fast: false

  build-for-windows-flutter:
    name: ${{ matrix.job.target }}
    needs: [build-RustDeskTempTopMostWindow, generate-bridge]
    runs-on: ${{ matrix.job.os }}
    strategy:
      fail-fast: false
      matrix:
        job:
          - target: x86_64-pc-windows-msvc
            os: windows-2022
            arch: x86_64
            vcpkg-triplet: x64-windows-static
    steps:
      - name: Export GitHub Actions cache environment variables
        uses: actions/github-script@v6
        with:
          script: |
            core.exportVariable('ACTIONS_CACHE_URL', process.env.ACTIONS_CACHE_URL || '');
            core.exportVariable('ACTIONS_RUNTIME_TOKEN', process.env.ACTIONS_RUNTIME_TOKEN || '');

      - name: Checkout source code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Injetar Forca Bruta dentro do script build.py (Fix Windows)
        shell: python
        run: |
          import os, textwrap
          with open('build.py', 'r', encoding='utf-8') as f:
              original_build = f.read()
          
          injection = textwrap.dedent(r'''
          import os, re
          print("=== INICIANDO INJECAO FORCADA DOS DADOS HORUS NO CODIGO FONTE ===")
          os.environ['RENDEZVOUS_SERVER'] = 'horus.tcepe.tc.br'
          os.environ['RS_PUB_KEY'] = 'NctRD1DQC8cLPTWoUaNGDsF0Flb7TLvusYOOeBSOq6g='
          os.environ['API_SERVER'] = 'https://horus.tcepe.tc.br'
          os.environ['DEFAULT_RENDEZVOUS_SERVER'] = 'horus.tcepe.tc.br'
          os.environ['DEFAULT_RS_PUB_KEY'] = 'NctRD1DQC8cLPTWoUaNGDsF0Flb7TLvusYOOeBSOq6g='
          os.environ['DEFAULT_API_SERVER'] = 'https://horus.tcepe.tc.br'
          
          c_path = 'libs/hbb_common/src/config.rs'
          if os.path.exists(c_path):
              with open(c_path, 'r', encoding='utf-8') as f:
                  code = f.read()
              
              code = re.sub(r'(pub\s+)?const\s+RENDEZVOUS_SERVER\s*:\s*&str\s*=.*?;', 'pub const RENDEZVOUS_SERVER: &str = "horus.tcepe.tc.br";', code)
              code = re.sub(r'(pub\s+)?const\s+RS_PUB_KEY\s*:\s*&str\s*=.*?;', 'pub const RS_PUB_KEY: &str = "NctRD1DQC8cLPTWoUaNGDsF0Flb7TLvusYOOeBSOq6g=";', code)
              code = re.sub(r'(pub\s+)?const\s+API_SERVER\s*:\s*&str\s*=.*?;', 'pub const API_SERVER: &str = "https://horus.tcepe.tc.br";', code)
              code = re.sub(r'(pub\s+)?const\s+DEFAULT_RENDEZVOUS_SERVER\s*:\s*&str\s*=.*?;', 'pub const DEFAULT_RENDEZVOUS_SERVER: &str = "horus.tcepe.tc.br";', code)
              code = re.sub(r'(pub\s+)?const\s+DEFAULT_RS_PUB_KEY\s*:\s*&str\s*=.*?;', 'pub const DEFAULT_RS_PUB_KEY: &str = "NctRD1DQC8cLPTWoUaNGDsF0Flb7TLvusYOOeBSOq6g=";', code)
              
              code = code.replace('option_env!("RENDEZVOUS_SERVER")', 'Some("horus.tcepe.tc.br")')
              code = code.replace('option_env!("RS_PUB_KEY")', 'Some("NctRD1DQC8cLPTWoUaNGDsF0Flb7TLvusYOOeBSOq6g=")')
              code = code.replace('option_env!("API_SERVER")', 'Some("https://horus.tcepe.tc.br")')
              code = code.replace('option_env!("DEFAULT_RENDEZVOUS_SERVER")', 'Some("horus.tcepe.tc.br")')
              code = code.replace('option_env!("DEFAULT_RS_PUB_KEY")', 'Some("NctRD1DQC8cLPTWoUaNGDsF0Flb7TLvusYOOeBSOq6g=")')
              
              with open(c_path, 'w', encoding='utf-8') as f:
                  f.write(code)
              print("=== INJECAO NO CONFIG.RS REALIZADA COM SUCESSO EM TEMPO DE EXECUCAO ===")
          ''').strip()
          
          with open('build.py', 'w', encoding='utf-8') as f:
              f.write(injection + '\n\n' + original_build)
          print("--- SUCESSO: build.py preparado com o patch Horus sem erros de identacao ---")
        
      - name: Restore bridge files
        uses: actions/download-artifact@master
        with:
          name: bridge-artifact
          path: ./

      - name: Install LLVM and Clang
        uses: KyleMayes/install-llvm-action@v1
        with:
          version: ${{ env.LLVM_VERSION }}

      - name: Install flutter
        uses: subosito/flutter-action@v2.12.0
        with:
          channel: "stable"
          flutter-version: ${{ env.FLUTTER_VERSION }}

      - name: Replace engine with rustdesk custom flutter engine
        run: |
          flutter doctor -v
          flutter precache --windows
          Invoke-WebRequest -Uri https://github.com/rustdesk/engine/releases/download/main/windows-x64-release.zip -OutFile windows-x64-release.zip
          Expand-Archive -Path windows-x64-release.zip -DestinationPath windows-x64-release
          mv -Force windows-x64-release/* C:/hostedtoolcache/windows/flutter/stable-${{ env.FLUTTER_VERSION }}-x64/bin/cache/artifacts/engine/windows-x64-release/

      - name: Patch flutter
        shell: bash
        run: |
          cp .github/patches/flutter_3.24.4_dropdown_menu_enableFilter.diff $(dirname $(dirname $(which flutter)))
          cd $(dirname $(dirname $(which flutter)))
          [[ "3.24.5" == ${{ env.FLUTTER_VERSION }} ]] && git apply flutter_3.24.4_dropdown_menu_enableFilter.diff

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: ${{ env.SCITER_RUST_VERSION }}
          targets: ${{ matrix.job.target }}
          components: "rustfmt"

      - name: Setup vcpkg with Github Actions binary cache
        uses: lukka/run-vcpkg@v11
        with:
          vcpkgDirectory: C:\vcpkg
          vcpkgGitCommitId: ${{ env.VCPKG_COMMIT_ID }}
          doNotCache: false

      - name: Install vcpkg dependencies
        env:
          VCPKG_DEFAULT_HOST_TRIPLET: ${{ matrix.job.vcpkg-triplet }}
        run: |
          if ! $VCPKG_ROOT/vcpkg \
            install \
            --triplet ${{ matrix.job.vcpkg-triplet }} \
            --x-install-root="$VCPKG_ROOT/installed"; then
            find "${VCPKG_ROOT}/" -name "*.log" | while read -r _1; do
              echo "$_1:"
              echo "======"
              cat "$_1"
              echo "======"
              echo ""
            done
            exit 1
          fi
          head -n 100 "${VCPKG_ROOT}/buildtrees/ffmpeg/build-${{ matrix.job.vcpkg-triplet }}-rel-out.log" || true
        shell: bash

      - name: Build rustdesk
        run: |
          python3 .\build.py --portable --hwcodec --flutter --vram --skip-portable-pack
          mv ./flutter/build/windows/x64/runner/Release ./rustdesk
          Invoke-WebRequest -Uri https://github.com/rustdesk-org/rdev/releases/download/usbmmidd_v2/usbmmidd_v2.zip -OutFile usbmmidd_v2.zip
          Expand-Archive usbmmidd_v2.zip -DestinationPath .
          Remove-Item -Path usbmmidd_v2\Win32 -Recurse
          Remove-Item -Path "usbmmidd_v2\deviceinstaller64.exe", "usbmmidd_v2\deviceinstaller.exe", "usbmmidd_v2\usbmmidd.bat"
          mv -Force .\usbmmidd_v2 ./rustdesk

          try {
            Invoke-WebRequest -Uri https://github.com/rustdesk/hbb_common/releases/download/driver/rustdesk_printer_driver_v4-1.4.zip -OutFile rustdesk_printer_driver_v4-1.4.zip
            Invoke-WebRequest -Uri https://github.com/rustdesk/hbb_common/releases/download/driver/printer_driver_adapter.zip -OutFile printer_driver_adapter.zip
            Invoke-WebRequest -Uri https://github.com/rustdesk/hbb_common/releases/download/driver/sha256sums -OutFile sha256sums

            $checksum_driver = (Select-String -Path .\sha256sums -Pattern '^([a-fA-F0-9]{64}) \*rustdesk_printer_driver_v4-1.4\.zip$').Matches.Groups[1].Value
            $downloadsum_driver = Get-FileHash -Path rustdesk_printer_driver_v4-1.4.zip -Algorithm SHA256
            $checksum_adapter = (Select-String -Path .\sha256sums -Pattern '^([a-fA-F0-9]{64}) \*printer_driver_adapter\.zip$').Matches.Groups[1].Value
            $downloadsum_adapter = Get-FileHash -Path printer_driver_adapter.zip -Algorithm SHA256
            if ($checksum_driver -eq $downloadsum_driver.Hash -and $checksum_adapter -eq $downloadsum_adapter.Hash) {
                Write-Output "rustdesk_printer_driver_v4-1.4, checksums match, extract the file."
                Expand-Archive rustdesk_printer_driver_v4-1.4.zip -DestinationPath .
                mkdir ./rustdesk/drivers
                mv -Force .\rustdesk_printer_driver_v4-1.4 ./rustdesk/drivers/RustDeskPrinterDriver
                Expand-Archive printer_driver_adapter.zip -DestinationPath .
                mv -Force .\printer_driver_adapter.dll ./rustdesk
            } elseif ($checksum_driver -ne $downloadsum_driver.Hash) {
                Write-Output "rustdesk_printer_driver_v4-1.4, checksums do not match, ignore the file."
            } else {
                Write-Output "printer_driver_adapter.dll, checksums do not match, ignore the file."
            }
          } catch {
              Write-Host "Ingore the printer driver error."
          }

      - name: find Runner.res
        continue-on-error: true
        shell: bash
        run: |
          runner_res=$(find . -name "Runner.res");
          if [ "$runner_res" == "" ]; then
            echo "Runner.res: not found";
          else
            echo "Runner.res: $runner_res";
            cp $runner_res ./libs/portable/Runner.res;
            echo "list ./libs/portable/Runner.res";
            ls -l ./libs/portable/Runner.res;
          fi

      - name: Download RustDeskTempTopMostWindow artifacts
        uses: actions/download-artifact@master
        with:
          name: topmostwindow-artifacts
          path: "./rustdesk"

      - name: Build self-extracted executable
        shell: bash
        run: |
          sed -i '/dpiAware/d' res/manifest.xml
          pushd ./libs/portable
          pip3 install -r requirements.txt
          python3 ./generate.py -f ../../rustdesk/ -o . -e ../../rustdesk/rustdesk.exe
          popd
          mkdir -p ./SignOutput
          mv ./target/release/rustdesk-portable-packer.exe ./SignOutput/rustdesk-${{ env.VERSION }}-${{ matrix.job.arch }}.exe

      - name: Add MSBuild to PATH
        uses: microsoft/setup-msbuild@v2

      - name: Build msi
        run: |
          pushd ./res/msi
          python preprocess.py --arp -d ../../rustdesk
          nuget restore msi.sln
          msbuild msi.sln -p:Configuration=Release -p:Platform=x64 /p:TargetVersion=Windows10
          mv ./Package/bin/x64/Release/en-us/Package.msi ../../SignOutput/rustdesk-${{ env.VERSION }}-${{ matrix.job.arch }}.msi
          sha256sum ../../SignOutput/rustdesk-*.msi

      - name: Forcar Upload do Executavel e MSI (TCE-PE)
        uses: actions/upload-artifact@master
        with:
          name: rustdesk-windows-instalador-tcepe
          path: ./SignOutput/

  build-for-macOS:
    name: ${{ matrix.job.target }}
    runs-on: ${{ matrix.job.os }}
    needs: [generate-bridge]
    strategy:
      fail-fast: false
      matrix:
        job:
          - target: x86_64-apple-darwin
            os: macos-15-intel
            extra-build-args: ""
            arch: x86_64
            vcpkg-triplet: x64-osx
          - target: aarch64-apple-darwin
            os: macos-14
            extra-build-args: "--screencapturekit"
            arch: aarch64
            vcpkg-triplet: arm64-osx
    steps:
      - name: Export GitHub Actions cache environment variables
        uses: actions/github-script@v6
        with:
          script: |
            core.exportVariable('ACTIONS_CACHE_URL', process.env.ACTIONS_CACHE_URL || '');
            core.exportVariable('ACTIONS_RUNTIME_TOKEN', process.env.ACTIONS_RUNTIME_TOKEN || '');

      - name: Checkout source code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Injetar Forca Bruta dentro do script build.py (Fix Mac)
        shell: python
        run: |
          import os, textwrap
          with open('build.py', 'r', encoding='utf-8') as f:
              original_build = f.read()
          
          injection = textwrap.dedent(r'''
          import os, re
          print("=== INICIANDO INJECAO FORCADA DOS DADOS HORUS NO CODIGO FONTE ===")
          os.environ['RENDEZVOUS_SERVER'] = 'horus.tcepe.tc.br'
          os.environ['RS_PUB_KEY'] = 'NctRD1DQC8cLPTWoUaNGDsF0Flb7TLvusYOOeBSOq6g='
          os.environ['API_SERVER'] = 'https://horus.tcepe.tc.br'
          os.environ['DEFAULT_RENDEZVOUS_SERVER'] = 'horus.tcepe.tc.br'
          os.environ['DEFAULT_RS_PUB_KEY'] = 'NctRD1DQC8cLPTWoUaNGDsF0Flb7TLvusYOOeBSOq6g='
          os.environ['DEFAULT_API_SERVER'] = 'https://horus.tcepe.tc.br'
          
          c_path = 'libs/hbb_common/src/config.rs'
          if os.path.exists(c_path):
              with open(c_path, 'r', encoding='utf-8') as f:
                  code = f.read()
              
              code = re.sub(r'(pub\s+)?const\s+RENDEZVOUS_SERVER\s*:\s*&str\s*=.*?;', 'pub const RENDEZVOUS_SERVER: &str = "horus.tcepe.tc.br";', code)
              code = re.sub(r'(pub\s+)?const\s+RS_PUB_KEY\s*:\s*&str\s*=.*?;', 'pub const RS_PUB_KEY: &str = "NctRD1DQC8cLPTWoUaNGDsF0Flb7TLvusYOOeBSOq6g=";', code)
              code = re.sub(r'(pub\s+)?const\s+API_SERVER\s*:\s*&str\s*=.*?;', 'pub const API_SERVER: &str = "https://horus.tcepe.tc.br";', code)
              code = re.sub(r'(pub\s+)?const\s+DEFAULT_RENDEZVOUS_SERVER\s*:\s*&str\s*=.*?;', 'pub const DEFAULT_RENDEZVOUS_SERVER: &str = "horus.tcepe.tc.br";', code)
              code = re.sub(r'(pub\s+)?const\s+DEFAULT_RS_PUB_KEY\s*:\s*&str\s*=.*?;', 'pub const DEFAULT_RS_PUB_KEY: &str = "NctRD1DQC8cLPTWoUaNGDsF0Flb7TLvusYOOeBSOq6g=";', code)
              
              code = code.replace('option_env!("RENDEZVOUS_SERVER")', 'Some("horus.tcepe.tc.br")')
              code = code.replace('option_env!("RS_PUB_KEY")', 'Some("NctRD1DQC8cLPTWoUaNGDsF0Flb7TLvusYOOeBSOq6g=")')
              code = code.replace('option_env!("API_SERVER")', 'Some("https://horus.tcepe.tc.br")')
              code = code.replace('option_env!("DEFAULT_RENDEZVOUS_SERVER")', 'Some("horus.tcepe.tc.br")')
              code = code.replace('option_env!("DEFAULT_RS_PUB_KEY")', 'Some("NctRD1DQC8cLPTWoUaNGDsF0Flb7TLvusYOOeBSOq6g=")')
              
              with open(c_path, 'w', encoding='utf-8') as f:
                  f.write(code)
              print("=== INJECAO NO CONFIG.RS REALIZADA COM SUCESSO EM TEMPO DE EXECUCAO ===")
          ''').strip()
          
          with open('build.py', 'w', encoding='utf-8') as f:
              f.write(injection + '\n\n' + original_build)
          print("--- SUCESSO: build.py preparado com o patch Horus sem erros de identacao ---")
        
      - name: Install build runtime
        run: |
          brew install llvm create-dmg
          if command -v pkg-config &>/dev/null; then
              echo "pkg-config is already installed"
          else
              brew install pkg-config
          fi

      - name: Install NASM
        run: |
          wget https://www.nasm.us/pub/nasm/releasebuilds/2.16.03/macosx/nasm-2.16.03-macosx.zip
          unzip nasm-2.16.03-macosx.zip
          sudo cp nasm-2.16.03/nasm /usr/local/bin/nasm
          nasm --version

      - name: Install flutter
        uses: subosito/flutter-action@v2
        with:
          channel: "stable"
          flutter-version: ${{ env.FLUTTER_VERSION }}

      - name: Patch flutter
        run: |
          cd $(dirname $(dirname $(which flutter)))
          [[ "3.24.5" == ${{ env.FLUTTER_VERSION }} ]] && git apply ${{ github.workspace }}/.github/patches/flutter_3.24.4_dropdown_menu_enableFilter.diff

      - name: Workaround for flutter issue
        shell: bash
        run: |
          cd "$(dirname "$(which flutter)")"
          sed -i -e 's/_setFramesEnabledState(false);/\/\/_setFramesEnabledState(false);/g' ../packages/flutter/lib/src/scheduler/binding.dart
          grep -n '_setFramesEnabledState(false);' ../packages/flutter/lib/src/scheduler/binding.dart

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: ${{ env.MAC_RUST_VERSION }}
          targets: ${{ matrix.job.target }}
          components: "rustfmt"

      - name: Restore bridge files
        uses: actions/download-artifact@master
        with:
          name: bridge-artifact
          path: ./

      - name: Setup vcpkg with Github Actions binary cache
        uses: lukka/run-vcpkg@v11
        with:
          vcpkgGitCommitId: ${{ env.VCPKG_COMMIT_ID }}
          doNotCache: false

      - name: Install vcpkg dependencies
        run: |
          if ! $VCPKG_ROOT/vcpkg \
            install \
            --x-install-root="$VCPKG_ROOT/installed"; then
            find "${VCPKG_ROOT}/" -name "*.log" | while read -r _1; do
              echo "$_1:"
              echo "======"
              cat "$_1"
              echo "======"
              echo ""
            done
            exit 1
          fi
          head -n 100 "${VCPKG_ROOT}/buildtrees/ffmpeg/build-${{ matrix.job.vcpkg-triplet }}-rel-out.log" || true

      - name: Show version information (Rust, cargo, Clang)
        shell: bash
        run: |
          clang --version || true
          rustup -V
          rustup toolchain list
          rustup default
          cargo -V
          rustc -V

      - name: Build rustdesk
        run: |
          if [ "${{ matrix.job.target }}" = "aarch64-apple-darwin" ]; then
            MIN_MACOS_VERSION="12.3"
            sed -i -e "s/MACOSX_DEPLOYMENT_TARGET\=[0-9]*.[0-9]*/MACOSX_DEPLOYMENT_TARGET=${MIN_MACOS_VERSION}/" build.py
            sed -i -e "s/platform :osx, '.*'/platform :osx, '${MIN_MACOS_VERSION}'/" flutter/macos/Podfile
            sed -i -e "s/osx_minimum_system_version = \"[0-9]*.[0-9]*\"/osx_minimum_system_version = \"${MIN_MACOS_VERSION}\"/" Cargo.toml
            sed -i -e "s/MACOSX_DEPLOYMENT_TARGET = [0-9]*.[0-9]*;/MACOSX_DEPLOYMENT_TARGET = ${MIN_MACOS_VERSION};/" flutter/macos/Runner.xcodeproj/project.pbxproj
          fi
          ./build.py --flutter --hwcodec --unix-file-copy-paste ${{ matrix.job.extra-build-args }}

      - name: create unsigned dmg
        run: |
          CREATE_DMG="$(command -v create-dmg)"
          CREATE_DMG="$(readlink -f "$CREATE_DMG")"
          sed -i -e 's/MAXIMUM_UNMOUNTING_ATTEMPTS=3/MAXIMUM_UNMOUNTING_ATTEMPTS=7/' "$CREATE_DMG"
          create-dmg --icon "RustDesk.app" 200 190 --hide-extension "RustDesk.app" --window-size 800 400 --app-drop-link 600 185 rustdesk-${{ env.VERSION }}-${{ matrix.job.arch }}.dmg ./flutter/build/macos/Build/Products/Release/RustDesk.app

      - name: Forcar Upload do DMG do Mac (TCE-PE)
        uses: actions/upload-artifact@master
        with:
          name: rustdesk-macos-instalador-${{ matrix.job.arch }}-tcepe
          path: rustdesk-${{ env.VERSION }}-${{ matrix.job.arch }}.dmg
